{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch neslogs (GDB)",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/neslogs",
      "args": [],
      "stopAtEntry": false,
      "cwd": "${workspaceFolder}",
      "environment": [],
      "externalConsole": false,
      "preLaunchTask": "Build (make)",
      "MIMode": "gdb",
      "miDebuggerPath": "/usr/bin/gdb",
      "setupCommands": [
        {
          "description": "Enable pretty-printing for gdb",
          "text": "-enable-pretty-printing",
          "ignoreFailures": true
        }
      ]
    }
  ]
}
